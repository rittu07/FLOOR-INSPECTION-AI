# Legacy synthetic dataset config (see data_floor_seg.yaml for the real-data training set)
# `path` is omitted so paths resolve relative to this file's directory.
train: datasets/crack_dataset/images/train
val: datasets/crack_dataset/images/val
test: datasets/crack_dataset/images/test

names:
  0: crack
